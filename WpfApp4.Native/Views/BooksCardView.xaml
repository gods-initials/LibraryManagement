<Window x:Class="WpfApp4.Views.BooksCardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4.Views"
        mc:Ignorable="d"
        Title="BooksCardView" Height="250" Width="250" WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="5 0 5 0">
            <Label Content="Id:"/>
            <TextBox Text="{Binding ReturnBook.Id}"/>
            <Label Content="Title:"/>
            <TextBox Text="{Binding ReturnBook.Title}"/>
            <Label Content="Author:"/>
            <TextBox Text="{Binding ReturnBook.Author}"/>
            <Label Content="Borrowed by:"/>
            <ComboBox ItemsSource="{Binding AvailableMembers}" SelectedItem="{Binding SelectedMember}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="OK" Height="25" Margin="5" Command="{Binding OKCommand}"/>
            <Button Content="Cancel" Height="25" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>