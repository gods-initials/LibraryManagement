<Window x:Class="LibraryManagement.Views.BooksCardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagement.Views"
        mc:Ignorable="d"
        Title="BooksCardView" Height="354" Width="250" WindowStyle="None" MouseDown="Window_MouseDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="5 0 5 0">
            <Label Content="Id:"/>
            <TextBox Text="{Binding ReturnBook.Id}"/>
            <Label Content="Title:"/>
            <TextBox Text="{Binding ReturnBook.Title}"/>
            <Label Content="Author:"/>
            <TextBox Text="{Binding ReturnBook.Author}"/>
            <Label Content="Borrowed by:"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <ComboBox ItemsSource="{Binding AvailableMembers}" SelectedItem="{Binding SelectedMember}" Width="190" Margin="0 0 5 0"/>
                <Button Content="X" Width="auto" Command="{Binding ReturnCommand}"/>
            </StackPanel>
            <Label Content="Due to:"/>
            <DatePicker SelectedDate="{Binding ReturnBook.DueTo}"/>
            <Label Content="Tags:"/>
            <TextBox IsReadOnly="True" Text="{Binding TagsString}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="OK" Height="25" Margin="5" Command="{Binding OKCommand}"/>
            <Button Content="Cancel" Height="25" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>