<Application x:Class="WpfApp4.Native.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp4.Native"
             xmlns:viewmodels="clr-namespace:WpfApp4.ViewModels"
             xmlns:views="clr-namespace:WpfApp4.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="auto"/>
            <Setter Property="Padding" Value="5 0 5 0"/>
        </Style>
        <DataTemplate DataType="{x:Type viewmodels:BooksTableViewModel}">
            <views:BooksTableView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MembersTableViewModel}">
            <views:MembersTableView/>
        </DataTemplate>
    </Application.Resources>
</Application>
